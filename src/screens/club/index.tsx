import React from "react";
import { View, Text, StyleSheet } from "react-native";
export const ClubScreen = () => (
  <View style={s.c}>
    <Text>câu lạc bộ</Text>
  </View>
);
const s = StyleSheet.create({
  c: { flex: 1, alignItems: "center", justifyContent: "center" },
});
